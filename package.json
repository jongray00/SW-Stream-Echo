{
  "name": "stream-echo",
  "version": "1.0.0",
  "description": "Use a stream to repeat back and echo the audio that was received",
  "main": "SWVoice_client.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node server.js",
    "start": "node run server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "ws": "^8.8.1"
  }
}
